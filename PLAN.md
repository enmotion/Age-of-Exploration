# Age of Exploration 开发计划

更新日期：2026-09-12

## 1. 项目定位与已确认决策

项目从用户主动清空后的目录重新建立，以 Babylon.js 为游戏引擎，开发单机航海游戏。旧项目仅可作为参考，不自动恢复旧文件或沿用 Three.js 实现。

以下内容为已确认需求；本文中的技术候选和阶段划分为实施建议，不代表尚未讨论的选型已经确定。

1. 使用 Babylon.js 开发游戏场景、渲染和交互。
2. 优先面向 Web 发行，架构预留 Windows exe 与 macOS dmg 桌面发行能力。本阶段不实现桌面打包，未来尽量通过少量配置与平台适配完成。
3. 建立完整资产管理管线，覆盖资产来源、授权记录、导入、加工、可视化选择、确认采用、修改与游戏使用。允许根据需要安装第三方软件或插件。
4. 贴图和材质可按实际需求生成或加工，并纳入统一资产规范。
5. 当前不开发后端，使用 IndexedDB 实现本地持久化。
6. 当前为单机游戏，未来可能接入后端，让部分 NPC 由模型扮演；现在预留接口，不实现远程模型服务。
7. 提供港口可视化编辑器，允许用户自行编辑、保存和预览港口。

## 2. 架构原则

### 2.1 游戏逻辑与引擎分离

- 航行、时间、经济、任务、NPC 状态等玩法逻辑尽量使用独立 TypeScript 数据结构，不直接依赖 Babylon.js 场景对象。
- Babylon.js 层负责场景创建、模型加载、材质、相机、光照、动画、特效、拾取和渲染资源释放。
- 游戏界面与编辑器通过明确接口操作游戏状态，避免直接修改引擎内部对象作为唯一数据来源。
- 港口定义与游戏存档保存可序列化数据，通过稳定标识引用资产，不将完整运行时场景作为唯一存储格式。
- UI 与构建已在初始化阶段确定为 Vue 3 + TypeScript + Vite，具体依赖版本见 package.json 与 package-lock.json。

### 2.2 Web 与桌面适配

- Web 版本为首要运行与验证目标。
- 将存储、文件导入导出、平台生命周期等能力集中在平台适配层。
- 避免在玩法代码中直接使用 Node.js 或桌面容器专有 API。
- Electron、Tauri 作为桌面容器候选，暂不确定最终选型，也不引入打包依赖。
- 未来桌面发行仍需验证系统兼容性、资源路径、安装升级，以及 Windows/macOS 签名和 macOS 公证等流程。少量配置接入不等于免除平台发行工作。

### 2.3 本地持久化

- 使用 IndexedDB 保存游戏进度、港口编辑数据、资产元信息和必要的用户设置。
- 通过存储接口访问数据，预留未来服务端实现；当前不实现账户、联网同步或多人功能。
- 设计数据库版本与数据结构迁移，支持存档及港口数据的导入、导出。
- 发布随附资产与用户导入资产分别管理：前者通过资源清单加载，后者按需保存 Blob 或派生文件，并处理浏览器配额及写入失败。
- 保存失败需反馈给用户，避免编辑内容在无提示情况下丢失。

### 2.4 NPC 扩展接口

- 当前 NPC 使用本地规则、脚本或状态机驱动。
- 将 NPC 决策提供者与游戏执行逻辑分离，未来可为部分 NPC 替换为后端模型服务。
- 决策接口使用明确的上下文、动作类型和参数；模型建议须经游戏规则校验后执行。
- 游戏状态由游戏系统维护，模型不能直接任意写入存档或场景。
- 为未来异步决策预留超时、取消和本地回退机制；本阶段只建立必要边界。
- 未来模型服务凭证由后端管理，不放入 Web 客户端。

## 3. 资产管理管线

### 3.1 来源与登记

资产可来自自行制作、授权素材库、购买素材和生成式工具。具体平台及素材包待美术方向确定后选择。

每项资产应记录：

- 稳定资产 ID、名称、分类、标签与版本。
- 来源链接、作者、获取方式、许可证及署名要求。
- 原始文件、加工文件与游戏使用文件之间的对应关系。
- 预览图、格式、尺寸，以及模型面数、贴图规格等必要信息。
- 当前状态，例如待审核、已采用、需修改、已弃用。
- 生成资产使用的工具、提示词及可获得的生成参数。

### 3.2 导入、加工与发布

建议工作流：来源登记 → 原文件归档 → 格式与授权检查 → 加工优化 → 生成预览 → 用户确认 → 加入游戏资产清单。

- 原始资产与运行时资产分开存放，保留原文件，便于重新加工。
- 模型优先评估 glTF/GLB，导入时统一单位、朝向、原点、命名及材质约定。
- 按需要进行模型简化、贴图压缩、碰撞体配置和 LOD 制作，具体预算通过目标设备实测确定。
- 建立可重复执行的处理和验证步骤，检查缺失引用、格式问题及超出预算的资产。
- 更新资产时保留稳定 ID，并检查现有港口引用是否仍然有效。

### 3.3 可视化资产库

资产库负责“哪些资源可以使用”，港口编辑器负责“资源放在哪里、具有何种游戏行为”。二者通过同一资产 ID 与清单连接。

资产库计划支持：

- 缩略图列表、分类、搜索与标签筛选。
- 模型旋转缩放预览，以及材质、尺寸和来源信息查看。
- 确认采用、标记需修改、弃用与版本替换。
- 调整导入缩放、方向及可支持的材质参数。
- 从资产库选择资源并放入港口。

复杂建模、拓扑、UV 与精细绘制交给外部工具处理；项目内不建设完整 DCC 软件。Blender 可作为模型加工候选，其他材质工具或插件按实际需要选择和安装。

### 3.4 贴图与材质

- 美术风格确定后统一分辨率、命名、色彩空间与 PBR 材质约定。
- 区分颜色、法线、粗糙度、金属度、环境遮蔽及自发光等贴图用途，不直接把普通生成图片当作所有材质通道使用。
- 生成或加工结果需检查平铺接缝、比例、光照一致性和实际模型效果。
- 保留可编辑来源及派生关系，支持重新生成与替换。

## 4. 港口编辑器

### 4.1 首版目标

- 新建、载入和保存港口。
- 从资产库选择建筑、码头、装饰等资源并摆放。
- 选择、移动、旋转、缩放、复制与删除对象。
- 提供基础吸附与属性面板。
- 配置出生点、交互点和 NPC 放置及必要属性。
- 支持撤销、重做，以及编辑内容的保存状态提示。
- 支持导入、导出港口定义。
- 从编辑模式进入游戏预览，验证位置、交互及场景表现。

### 4.2 数据与运行约束

- 港口定义包含格式版本、对象 ID、资产引用、变换及行为配置。
- 编辑器和游戏使用同一套港口加载逻辑，避免预览与运行结果不一致。
- 编辑器辅助线、操纵器和选择标记不进入正式游戏数据。
- 预览期间的临时游戏状态不应无意覆盖港口设计数据。
- 保存或导入时校验数据结构与资产引用，对缺失资产提供可理解的提示。

地形雕刻、复杂道路生成和完整导航网格编辑暂不纳入已确认首版范围，后续按需要讨论。

## 5. 建议实施阶段与验收

### 阶段一：工程与基础边界

- 初始化 Babylon.js 与 TypeScript 工程，确定 UI 和构建工具。
- 建立游戏逻辑、引擎、界面、资产、存储和平台适配的模块边界。
- 建立最小启动场景、资源加载与释放流程。
- 建立 IndexedDB 数据版本管理和基础存取能力。

验收：Web 开发与生产构建可运行，基础数据可保存并在刷新后恢复，场景可正常创建与销毁。

### 阶段二：资产库与港口编辑闭环

- 使用少量来源明确的占位资产，建立清单、导入规范与预览流程。
- 实现可视化资产选择和确认采用。
- 实现港口基础编辑、属性调整、撤销重做及持久化。
- 实现港口导入导出与游戏预览。

验收：用户能够选取资产、搭建一个港口、保存、重新打开并进入预览；资产缺失或数据错误有明确提示。

### 阶段三：最小航海玩法

- 实现船只加载、相机、输入控制与基础航行。
- 打通港口与航海场景的必要衔接。
- 加入本地 NPC 行为和基础交互，落实可替换的决策接口。
- 打通游戏进度存取。

验收：形成可重复游玩的最小流程，重新载入能够恢复约定的游戏状态。

### 阶段四：视觉与资产质量

- 根据确定的风格制作或生成贴图、材质，替换占位资产。
- 分步建设海面、船体起伏、尾流、昼夜等环境表现；风暴等效果按玩法需要安排。
- 使用代表性场景检查性能、加载时间与内存，形成资产预算。

验收：代表性航海与港口场景达到约定的视觉和性能目标，资产来源及加工记录完整。

### 后续阶段：桌面发行与后端扩展

- 选择并接入桌面容器，完成 exe/dmg 构建及平台验证。
- 如决定引入模型 NPC，再设计后端协议、服务和对应决策适配器。

本阶段不实施以上两项，只维护必要的架构边界。

## 6. 验证策略

- 对航行等关键纯逻辑、存档迁移和编辑器撤销重做进行有意义的自动化验证。
- 验证资产引用、港口导入导出和损坏数据处理。
- 对资产预览、港口编辑、游戏预览、刷新后恢复进行浏览器流程验证。
- 在代表性场景检查渲染效果、资源释放和性能，不假设更换引擎即可自动提高画质或帧率。
- 目标浏览器、设备、帧率和内存预算待确定后成为具体验收标准。

## 7. 尚待确定的事项

以下问题不阻碍建立工程基础，但应在相关功能实施前确定：

- 美术方向：写实、风格化或低多边形，以及参考作品。
- 世界组织：真实地理地图、自建世界，或两者结合；目前不默认继续使用 Mapbox。
- 港口规模、地形需求、室内场景与 NPC 数量预期。
- 首版核心玩法循环及功能优先级。
- 目标浏览器、最低设备配置、移动端支持范围及性能目标。
- 资产工具组合、素材采购预算与桌面容器选型。

## 8. 当前范围

当前已进入阶段一：工程初始化。无需恢复旧项目，不提前实现后端、多人系统、模型服务或桌面安装包，也不预先采购素材或安装第三方资产制作软件。

### 初始化实施记录

- 采用 Vue 3 + TypeScript + Vite，Babylon.js 独立管理场景，依赖版本固定并记录于 lockfile。
- 采用 Dexie 封装 IndexedDB，通过仓储接口访问；数据库与港口文档版本均从 1 开始。
- 建立基础港口预览、相机控制、场景销毁重建、名称保存恢复及 JSON 导出。
- 建立程序化海岛、码头和帆船示例，登记稳定资产 ID 与代码来源；未引入外部美术资产。
- 建立平台服务接口、引擎无关港口数据、文档校验和本地 NPC 决策接口。
- 加入格式、lint、类型检查、存储与文档测试、浏览器流程测试和 CI 配置。
- 完整资产库、港口编辑器、JSON 导入界面、游戏进度表及数据版本升级迁移仍随对应后续功能实施。

上述 UI 与构建选型已在本次初始化中确定；桌面容器、美术风格和世界组织方式仍待确定。

### 全屏海洋场景调整（2026-09-12）

- 当前入口改为占满浏览器视口的海洋场景，移除港口示例、标题栏、资产列表和存档界面，只保留可折叠的水面效果控件。
- 视觉方向参考 Popov72 OceanDemo；当前为 WebGL 多尺度解析波浪方案，并非参考案例 WebGPU FFT 模拟的完整移植。
- 支持波浪位移、细节法线、天空反射、太阳高光、浪尖泡沫和远景雾化；水面和天空均程序化生成，无新增外部美术资源。
- 控件实时调节浪高、浪尖锐度、流动速度、风向、细碎波纹、泡沫、粗糙度、水色、阳光角度和曝光，提供平静、海风、涌浪预设及暂停、恢复默认。参数暂不持久化。
- 港口领域数据、资产登记和 IndexedDB 存储接口继续保留，为后续港口编辑器使用。
